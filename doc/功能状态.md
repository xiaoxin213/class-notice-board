# 功能状态

> 整理日期：2026-09-08
> ✅ 已完成并可用　🔲 待实现　💡 可优化（已可用但有提升空间）

---

## 服务端

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| 教师账号注册（邀请码制） | ✅ | 邀请码由 `INVITE_CODE` 环境变量或管理后台动态配置 |
| 教师账号登录 | ✅ | JWT，默认 30 天有效期 |
| 停用账号时阻止登录 | ✅ | disabled 字段，登录返回 403 |
| 班级 CRUD（含改名/删除） | ✅ | owner/assistant 双角色 |
| 教室绑定码生成（6位，30分钟有效） | ✅ | 多次生成覆盖旧码 |
| 教室端设备绑定（无需登录） | ✅ | deviceToken 长期凭据 |
| 同名设备复用（换机不堆积） | ✅ | 按 class_id + name 唯一 |
| 设备在线状态（进程内存） | ✅ | 重启后教室端重连自动恢复 |
| 删除设备（离线状态才允许） | ✅ | |
| 发布通知 | ✅ | 同步返回，TTL 5 分钟 |
| 通知送达回执（ACK） | ✅ | 教室端展示后回 ack，状态变 delivered |
| 离线补投（TTL 内） | ✅ | 设备上线时自动 deliverPending |
| 通知记录查询（最近 30 条） | ✅ | 含 publisher、status |
| SSE 在线状态推送 | ✅ | device_status 事件 |
| SSE 送达通知推送 | ✅ | notice_ack 事件 |
| 声音测试 | ✅ | 向在线教室端推 sound_test 帧 |
| 内建静态文件服务（省 nginx 容器） | ✅ | SPA 回退（非 /api、非 /ws 路径返回 index.html） |
| 健康检查 `/health` | ✅ | 含在线设备数 |
| 管理后台：用户列表与统计 | ✅ | GET /api/admin/stats |
| 管理后台：注册邀请码管理 | ✅ | GET/PUT /api/admin/settings |
| 管理后台：改用户姓名/密码 | ✅ | PATCH /api/admin/teachers/:id |
| 管理后台：停用/启用/删除用户 | ✅ | |
| 管理后台：安装包下载列表 | ✅ | 扫描 WEB_ROOT/downloads/ |
| 绑定码防爆破限流 | ✅ | 同 IP 10 分钟失败 10 次锁定 |
| 多班级群发 / 全校广播 | 🔲 | 待定，PRD 未确认 |
| 通知撤回 | 🔲 | 待定 |
| 忘记密码 / 邮件重置 | 🔲 | 目前只能管理员后台改 |
| 注册开关（reg_open setting） | 💡 | 字段已在 setting 表定义，API 尚未暴露 reg_open 控制 |
| 通知内容长度 | 💡 | 硬限 200 字但无截断提示，建议前端实时计数并软性引导 |

---

## 教师端 Web

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| 登录 / 注册（双 Tab 切换） | ✅ | |
| 落地页（`/`）产品介绍 | ✅ | 动画通知演示、特性介绍、下载区 |
| 落地页登录弹窗 | ✅ | 登录后跳转 /console |
| 已登录自动跳 /console | ✅ | |
| 工作台（`/console`）三栏布局 | ✅ | 班级列表 / 发布区 / 绑定区 |
| 班级列表（含配额显示） | ✅ | 新建班级、选中切换 |
| 在线/离线状态点实时刷新 | ✅ | SSE device_status 驱动 |
| 发布通知（内容 + 显示时长 + 播报次数） | ✅ | |
| 常用模板一键填充 | ✅ | 4 个预设模板 |
| 字数计数（实时） | ✅ | 21/200 样式 |
| 大屏预览（不真实发布） | ✅ | 弹窗预览教室端效果 |
| 发布记录（最近通知） | ✅ | 含送达状态 |
| 绑定码生成 + 复制 | ✅ | |
| 声音测试 | ✅ | |
| 设备在线状态（分班级） | ✅ | |
| 退出登录 | ✅ | |
| 管理后台（用户管理、设置） | ✅ | isAdmin 时显示 |
| 响应式（移动端适配） | 💡 | 基本可用，断点 768px；三栏在手机上需进一步优化 |
| 多班级群发 | 🔲 | |
| 发布记录分页 / 搜索 | 🔲 | 目前只展示最近 1 条 |
| PWA 离线缓存 | 🔲 | |

---

## 教室端 Electron

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| 首次启动配置向导（服务器地址 + 绑定码） | ✅ | |
| WebSocket 长连接（deviceToken 认证） | ✅ | |
| 指数退避重连（±20% 抖动） | ✅ | 1s → 2s → 4s … 上限 30s |
| 全屏通知展示 | ✅ | 超大字号，居中 |
| 语音播报（本地 speechSynthesis） | ✅ | 支持播报次数 |
| 回执 ACK | ✅ | 展示后回 ack |
| 声音测试响应 | ✅ | |
| 通知自动关闭（displaySeconds） | ✅ | |
| 手动关闭通知（点击或 ✕ 按钮） | ✅ | |
| `expireAt` 本地过期丢弃 | ✅ | |
| 按 notice.id 去重 | ✅ | |
| config.txt 支持（UTF-8 + GBK） | ✅ | |
| 托盘菜单（重新配置 / 退出） | ✅ | |
| Windows 安装包（NSIS，per-user） | ✅ | GitHub Actions windows-latest 构建 |
| macOS 安装包（DMG，x64+arm64） | ✅ | GitHub Actions macos-latest 构建 |
| Win7 32 位兼容 | ✅ | Electron 22.3.x 上限 |
| 待机态（通知关闭后的界面） | 💡 | 目前返回空白/底色，可设计待机画面 |
| 多屏支持（教室内多台设备同时绑定） | ✅ | 一个班级可绑多设备 |
| 离线时自动重连后补投通知 | ✅ | TTL 内有效 |

---

## CI / CD

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| Docker 镜像自动构建（GHCR） | ✅ | main 分支 push 触发 |
| Windows 客户端自动构建 | ✅ | windows-latest runner |
| macOS 客户端自动构建 | ✅ | macos-latest runner，手动触发 |
| 自动发布 Release | 🔲 | 目前只构建产物，未自动创建 GitHub Release |
| 版本号自动递增 | 🔲 | `npm version patch` 需手动执行 |
